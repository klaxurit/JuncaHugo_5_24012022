{% extends "admin/base.html.twig" %}

{% block title %}
{% if social is empty %}
	Blog - Ajout d'un réseau social
	{% else %}
	Blog - Modification d'un réseau social
{% endif %}

{% endblock %}
{% block body %}
	<div class="admin-container">
	{% if social is empty %}
		<h3 class="admin__manage-title">Update social
		</h3>
	{% else %}
			<h3 class="admin__manage-title">Create social
		</h3>
	{% endif %}
			
		<form method="post" action="">
			<div class="form-group">
				<label for="iconName">Icon name*</label>
				{% if social is empty %}
				<input type="text" name="iconName" class="form-control" id="iconName" value="{{ _post.iconName }}">
				{% else %}
				<input type="text" name="iconName" class="form-control" id="iconName" value="{{ social.iconName }}">
				{% endif %}
				{% if errors['iconName'] is defined %}
					<p class="form_error">{{ errors['iconName'] }}</p>
				{% endif %}
			</div>
			<div class="form-group">
				<label for="socialUrl">Url</label>
				{% if social is empty %}
				<input type="text" name="url" class="form-control" id="socialUrl" value="{{ _post.url }}">
				{% else %}
				<input type="text" name="url" class="form-control" id="socialUrl"  value="{{ social.url }}">
				{% endif %}
				{% if errors['url'] is defined %}
					<p class="form_error">{{ errors['url'] }}</p>
				{% endif %}
			</div>
			<div class="form-group">
				<label for="socialName">Name</label>
				{% if social is empty %}
				<input type="text" name="name" class="form-control" id="socialName" value="{{ _post.name }}">
				{% else %}
				<input type="text" name="name" class="form-control" id="socialName" value="{{ social.name }}">
				{% endif %}
				{% if errors['name'] is defined %}
					<p class="form_error">{{ errors['name'] }}</p>
				{% endif %}
			</div>
			<button type="submit" class="btn btn-primary">Modifier</button>
		</form>
		<p class="social__info">*To find the "Icon name" go on https://icones.js.org/ and click on the icon needed.<br>Example for instagram logo: icon-park-outline:instagram </p>
	</div>
{% endblock %}