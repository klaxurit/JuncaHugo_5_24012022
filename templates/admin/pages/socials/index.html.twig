{% extends "admin/base.html.twig" %}

{% block title %}
	Blog - Gestion des réseaux sociaux
{% endblock %}
{% block body %}
	<div class="admin-container">
		<h3 class="admin__manage-title">Gestion des réseaux sociaux</h3>
		<a href="/admin/socials/create" class="btn btn-sm btn-primary mb-4">Ajouter un réseau social</a>
		<table class="table table-striped">
			<thead>
				<th>ID</th>
				<th>Nom de l'icône</th>
				<th>URL</th>
				<th>Nom du résau social</th>
				<th>Action</th>
			</thead>
			<tbody>
				{% for social in socials %}
					<tr>
						<td>{{ social.id }}</td>
						<td>{{ social.iconName }}</td>
						<td>{{ social.url }}</td>
						<td>{{ social.name }}</td>
						<td class="mt-2 pt-2">
							<a href="/admin/socials/update/{{ social.id }}" class="btn btn-sm btn-warning">Modifier</a>
							<a href="/admin/socials/delete/{{ social.id }}" onclick="return confirm('Ettes-vous sûr de vouloir supprimer ce réseau social ?')" class="btn btn-sm btn-danger">Supprimer</a>
						</td>
						<td>
						</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
		<input type="button" class="btn btn-sm btn-secondary" value="Retour" onClick="javascript:history.go(-1)" />
	</div>
{% endblock %}