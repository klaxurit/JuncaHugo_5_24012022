<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
	<a class="navbar-brand" href="/admin">Pannel d'administration</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarSupportedContent">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item">
				<a class="nav-link" href="/">Accueil du site</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="/blog">Liste des articles</a>
			</li>
		</ul>
		<ul class="navbar-nav ml-auto">
			<li class="nav-item">
				<a class="nav-link" href="/logout">Déconnexion</a>
			</li>
		</ul>
	</div>
</nav>
<div class="container">
	<pre>
    {{ dump(_session["FLASH_MESSAGES"]["comment"]["type"] is same as("success")) }}
    </pre>
	{% if _session["FLASH_MESSAGES"]["comment"]["type"] is defined %}
		{% if _session["FLASH_MESSAGES"]["comment"]["type"] is same as("error") %}
			<div class="alert alert-danger" role="alert">
				{{ _session["FLASH_MESSAGES"]["comment"]["message"] }}
			</div>
		{% endif %}
	{% endif %}
	{% if _session["FLASH_MESSAGES"]["comment"]["type"] is defined %}
		{% if _session["FLASH_MESSAGES"]["comment"]["type"] is same as("success") %}
			<div class="alert alert-success" role="alert">
				{{ _session["FLASH_MESSAGES"]["comment"]["message"] }}
			</div>
		{% endif %}
	{% endif %}
	{{ contenu }}
</div>
